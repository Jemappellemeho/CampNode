# Kopiere diese Datei zu .env (Entwicklung) bzw. .env.production (Produktion)
# und passe die Werte an.

# Lokale Entwicklung: Backend direkt auf localhost:3000
# Produktion: leer lassen → api.ts nutzt '/api' → Caddy routet intern
VITE_API_URL=http://localhost:3000/api
